This directory is used for example purposes.

It sets up the user's catalog_search_path directory for databroker,
copies a catalog configuration to that directory,
and copies msgpack catalogs to that directory as well.
